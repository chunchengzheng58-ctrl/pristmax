# Claude Code：请先读最新交接

本项目官网源码是 `site/`。继续官网或部署前，先读 `DEPLOY-CLAUDE-LATEST.md`；该文件覆盖 `HANDOFF-CLAUDE.md` 中过期的提交号、页脚设计和“尚未上线”说明。

不要覆盖用户或其他代理的未提交修改。不要 `git add .`、reset、clean，也不要部署仓库根目录。官网和 GitHub Pages 是两个发布入口，必须分别发布、分别验证。
