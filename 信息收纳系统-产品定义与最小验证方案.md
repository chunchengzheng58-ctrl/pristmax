# 信息收纳系统：产品定义与最小验证方案

本文根据用户的产品设想整理。市场规模、竞品、客户付费意愿及压缩收益尚未核验；下面的场景选择和产品方案是待验证的设计假设。

## 1. 产品要解决的问题

人们经常为了保留少量信息，保存整个文件。例如，拍摄一页 PPT 是为了记住知识点，保存截图是为了留下时间、地点或操作步骤。保存的动作很容易，事后判断、整理和寻找却很费力。

文件逐渐堆积，产生三项成本：存储占用、寻找时间，以及逐个决定去留的认知负担。

产品目标：在保留用户所需信息的前提下，自动整理数据、移除重复表示、选择更紧凑的保存方式，使相同容量能够承载更多可用的信息。

这里的“有效容量”是相对于明确用途而言的，不是硬盘物理容量增加。未来用途未知时，系统无法保证今天判断为不重要的信息永远不重要。

## 2. 用户价值与边界

产品承诺：随手存下的资料，自动变得好找、好用、少占空间。

不以“所有数据都要完整保存”作为默认产品目标，也不以“没有打开过就是无用”作为删除依据。未使用的数据可能是备用信息；频繁使用的数据也可能存在大量重复。

将两个维度分开：

- 信息是否值得保留：取决于用途、时效、稀缺性、能否重新获得。
- 当前表示是否值得保留：用户需要文本，还是也需要版面、图表、画面细节或原始文件。

允许用户对不同类别设置一次规则，由系统处理大部分文件，只将少量不确定项目集中交给用户复核。不要把整个相册变成另一张需要逐项处理的待办清单。

## 3. 第一个场景：学习资料照片与截图

初期只处理用户主动导入的 PPT、白板、通知和学习截图。这样可以先验证信息提取、检索和空间收益，无须立即承担整个相册的复杂分类。

从输入到结果的示例：

1. 用户导入一批课堂照片与截图。
2. 系统识别文字、标题、列表、公式、图表和重复内容。
3. 纯文字区域转成结构化文本；必要的图表保留裁剪图；公式识别不可靠时保留对应图块。
4. 生成可检索的信息卡片，并保留来源、拍摄时间、页序和提取状态。
5. 低置信度数字、识别失败区域及复杂版面进入少量例外复核。
6. 用户看到处理前后占用与原图对照，确认一批规则后再执行原文件替换或清理。

默认采用可撤销的试运行阶段。只有原文件及相关回收站占用被实际释放，才计入已释放空间；仍保留原件时，展示预计可释放空间。

日常查看直接显示文本、图表和来源信息。学习资料没有必要每次都调用生成模型重新绘制一张照片。

## 4. 保存方式

| 保存方式 | 适用情况 | 保存内容 |
| --- | --- | --- |
| 去重引用 | 内容完全相同的文件 | 单份内容、多个来源引用 |
| 信息提取 | 纯文字通知、简单 PPT | 结构化文本、必要元数据、少量缩略图 |
| 混合保存 | 含图表、公式、复杂布局的资料 | 文本、关键图块、页序和关联关系 |
| 普通转码 | 用户仍需要观看或收听的媒体 | 用户接受质量水平下的媒体文件 |
| 摘要归档 | 明确只需要要点的长内容 | 摘要、结构化事实、时间或来源标记 |
| 原件保留 | 无法可靠提取或用途需要原件 | 原件及可选索引 |

精确数字、否定词、单位和日期不能因为用户接受低画质就被模糊处理。视觉保真、事实保真和文件比特一致是三种不同要求。

哈希只能用于比对，不能恢复删除的原文。嵌入向量主要用于检索，不是原件的可逆替代品。只有完整、正确的编码与可用解码依赖才能承担精确恢复。

## 5. 自动决策机制

先识别内容类型，再判断用户需要的保真度，最后选择成本最低且满足要求的表示。

决策依据包括：用户对该类资料设定的规则、OCR 与版面识别质量、是否包含图表或公式、内容重复程度、过期时间，以及替换后实际能省多少空间。

访问频率只作为提示，不作为自动删除的充分条件。相似文件也不能直接当成重复文件：一页幻灯片可能只改了一个考试日期。

设置保底逻辑：提取不完整、转换后更大、识别结果无法满足用途时，保留原件，不执行替换。

## 6. 技术最小闭环

导入 → 内容哈希与重复检测 → OCR/版面分析 → 图表与公式区域保留 → 信息卡片生成 → 全文检索 → 原图对照 → 保存策略预览 → 经授权替换 → 统计实际空间。

数据模型至少包含：来源 ID、原文件哈希、来源路径或导入标识、内容类型、文本、关键图块、提取质量标记、保存策略、处理版本、实际字节数和原件保留状态。

第一版优先全文检索。语义向量索引与大模型分类在确实改善检索或决策时再加入，避免它们的存储与计算成本超过收益。

先用本地导入目录实现可测量的原型；手机系统照片库的权限、回收站机制和云同步行为需要在选择平台后单独验证。

## 7. 如何证明有效

使用经同意提供的真实资料样本，覆盖纯文字、图表、公式、模糊照片、重复截图与少量高风险内容。样本量及用户数量根据招募条件确定，不将小规模试验当成市场证明。

同时比较原文件、常规缩图或转码、信息提取三种保存方式，避免把本来可以通过简单压缩获得的收益误归因于 AI。

衡量指标：

- 净空间收益：处理前占用减去处理后的全部占用，包括文本、图块、索引、元数据、缓存和仍保留的原件。共享模型另列固定成本，按部署范围摊销。
- 信息可用性：用户能否找到并正确回答自己需要的问题，关键数字、公式和图表是否保留正确。
- 整理负担：每批需要人工检查的项目数、人工耗时、放弃率。
- 使用体验：检索成功率、找到资料所需时间、用户是否愿意继续导入。
- 恢复与信任：撤销请求、丢失信息反馈、用户愿意采用自动规则的范围。
- 处理成本：计算时间、费用、能耗与等待时间。

示例收益仅用于解释口径：假设 100 张图共 400 MB，提取结果加索引共 10 MB，且原件已实际释放，则释放约 390 MB。如果原件仍保留，项目总占用反而是约 410 MB；只能说生成了更小的替代表示。

## 8. 个人与企业的关系

二者可以共享内容识别、提取、去重、策略与检索引擎，但初期应分别验证工作流程与价值。

个人用户的重点是少整理、容易找、设备空间确实释放。企业需要按指定数据集合验证权限、保留规则、访问方式、备份同步和迁移成本。

企业节省的是可以实际减少的总成本：存储、备份、传输和检索成本的下降，减去新增的推理、索引、运维、迁移与复核成本。在已有固定容量合同下，释放空间未必马上降低账单，也可能体现为延迟扩容。

企业首个试点可从经客户确认、允许替换表示的内部培训截图或参考资料开始。不得把个人用户接受的处理方式直接套到所有企业文件。

## 9. 后续扩展

在照片资料的闭环成立后，按用途扩展：会议录音可保留转录、说话人和必要片段；课程视频可保留章节、讲稿、关键画面和演示片段；AI 生成文件可进行版本差异存储或保存可复现输入；重复文档可做内容级去重。

音频转录不能自动替代声学内容，文本摘要不能自动替代原文，生成配方不能自动保证恢复原输出。每个新类型都要重新验证“需要保留什么”。

## 10. 当前待验证的核心假设

1. 目标用户确实积累了足够多适合替换表示的文件。
2. 提取后的信息卡片比原来的文件更便于使用。
3. 用户愿意对低风险类别授权批量替换，而不仅仅使用搜索功能。
4. 扣除新增数据与处理成本后，净收益仍足够明显。
5. 少量例外复核就能维持可靠性，不会重新制造整理负担。

最关键的产品问题：能否让用户无需逐张判断，就放心地把一批资料交给系统处理，并在日后需要时找回正确的信息。
