# 企业无损保管试点

本次新增独立的「企业无损保管」入口，使用 deep_archive.py 的自包含归档引擎。未接入尚未通过独立恢复验证的 storage_archive.py、storage_restore.py 分布式原型。

## 操作

1. 在企业自己的机器上运行 `python app.py --port 8768`，打开本机地址 http://127.0.0.1:8768。旧版进程不会自动加载新后端代码，也不遵守本次新增的进程锁；勿从旧版服务操作同一保管任务。
2. 进入「企业无损保管」，填写独立资料目录，点击「无损折叠并验证」。系统不上传内容，不持久保存本次扫描报告。
3. 填写保管区之外、已经存在的交付目录，点击「还原到指定目录」。系统创建独立子目录，不覆盖现有文件。还原不依赖源文件。
4. 检查交付文件，勾选收妥确认，点击「重新核验并完成取走」。系统重新计算所有交付文件的 SHA-256 和长度，通过后删除本任务归档、归档清单及回执。
5. 完成后只保留保管编号、状态、文件数量等最小记录；交付文件和源目录保持不动。

恢复失败、文件丢失或内容变化时拒绝清理。清理中断保留过渡记录，可重启后重试；过渡记录在完成前含有恢复清单。

## 承诺范围

- 新保管任务保证普通文件字节、相对路径与修改时间；旧归档仍可读取，但无法补回未保存的修改时间。暂不保存 ACL、创建时间、空目录、扩展属性、备用数据流等完整文件系统元数据。目录中有被扫描规则排除的文件时，拒绝创建不完整保管任务。不能作为系统盘迁移工具。
- 当前保留源文件，不自动释放原始目录空间。归档新增占用与潜在压缩收益须区分。
- 单批上限 2000 个文件、20GB；无净压缩收益则拒绝保管。当前本地/NAS 支持，云端仍为只读盘点。
- 当前未实现加密、企业身份认证或远程托管。应在企业控制的本机试点，不对外暴露服务。
- 「清理」仅指本任务应用管理的文件删除，不证明 SSD 物理擦除，也不覆盖操作系统快照、云版本、外部备份、用户下载副本或旧版任务。
- 系统不创建本任务的 OCR、摘要或 AI 重建副本；原有其他功能生成的历史资料不受本任务控制。
- 可通过环境变量 `STORAGE_ATLAS_DATA` 指定企业自己的工作目录。新版所有保管操作共用操作系统文件锁，其他进程冲突时立即拒绝，进程退出后释放。仍推荐单服务部署；尚未验证 NAS/网络文件系统的锁语义。

## 验证

`python -m unittest discover -s tests -v`

本次 46 项测试通过。新增状态替换失败、进程互斥及退出释放锁、磁盘不足、写回失败、校验与恢复之间归档替换、清理前状态写入失败、恶意清单与恢复体积限制，以及混合随机二进制的精确恢复验证。保留此前选择性取出、修改时间、全量巡检与 HTTP 端到端覆盖。

## 稳定性与无损边界

- 状态先写唯一临时文件，刷新缓冲并 fsync，然后原子替换；替换失败保留旧状态，不继续清理归档。
- 归档及回执同步后提交。恢复先写 `.pending-编号` 临时目录；全部文件内容、修改时间验证及同步成功后改名为正式交付目录。发生正常可捕获异常时清理本次临时目录。
- 强制结束进程可能留下 `.pending-*` 目录，不能作为已交付文件使用；当前不自动删除这类残留。若正式交付目录发布后状态写入失败，也可能留下完整但未登记的交付目录。源文件与归档继续保留，由管理员核对。
- 恢复开始前检查预计写入量与可用空间，预留 1MiB；不能保证随后空间不被其他进程占用。运行中 I/O 错误仍按失败处理。
- 将先前验证的完整清单绑定到恢复步骤，第二次读取检查清单、成员类型、数量及大小，最后核对输出内容，拒绝用另一份合法归档替换原归档。
- 清单限定 2000 个文件、20GB 展开体积，拒绝文件与目录路径冲突、保留名称及错误字段类型。
- 该流程不调用生成模型，不接受语义相似替代字节一致。未承诺 ACL、所有扩展元数据无损，也未完成真实断电、大规模压力、介质故障或网络存储测试。Windows 下没有使用可移植的目录 fsync；硬件缓存与文件系统会影响真实掉电持久性。
- `.operation.lock` 仅是进程协调文件，不含企业文件内容，保留它以避免锁文件替换导致并发失效。

## 新增企业能力

- **归档目录与选择性取出**：点击「查看目录并选择文件」，按路径筛选、勾选文件，填写交付目录后取出。只写入所选文件，归档和保管状态不变。产生的交付文件由企业管理，不纳入保管区清理。
- **全量完整性巡检**：核对新任务存入时的归档 SHA-256，再检查所有内容块与清单；结果和耗时保存到任务状态。失败不会自动删除任何数据。旧任务无基准指纹时只能检查内部完整性。
- **修改时间保留**：新保管任务把每个文件的修改时间写入自包含清单；恢复后检验目标文件系统是否精确保留。完整交接清理前再次核对。

巡检指纹保存在本机状态文件中，不是抵御管理员同时篡改归档与状态的签名证据。当前巡检由操作者主动触发，尚无定时调度。整批压缩仍需顺序解压读取，选择性取出减少写盘量，不承诺随机读取或更低延迟。没有本次压缩率提升的实测结论。
