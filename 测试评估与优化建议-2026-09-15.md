# 存序当前版本测试评估与优化建议

测试日期：2026-09-15。对象是当前工作区代码，包括新增分布式、CDC、归档与恢复模块。本轮只新增评估脚本与本文档，没有修改业务实现。

## 结论

旧的 `deep_archive.py` 单机深度归档具备可验证的压缩和独立恢复能力；相似文本、版本化文件是已测出的有效场景。新增分布式和恢复模块仍包含占位实现与严重正确性问题，不能依据 README 将其视为已完成 PB 级系统，更不能据此清理唯一原件。

## 测试方法与范围

- 原有 24 项 unittest 测试全部通过，但这些测试主要覆盖原单机流程。
- 新增 `scripts/evaluate_current.py`，生成六类测试资料，比较旧基线和深度归档，并复现新增模块故障。
- 全部输入由脚本生成；独立恢复时将该次测试的输入目录移到同一测试工作目录内，保留字节但让原路径不可访问，没有删除客户数据。
- 结果：`output/evaluation/1c2bfe0d87bd4a8a966923d463f0fa2d/results.json`。
- 没有真实客户资料、PB 级规模、云端账单、ScyllaDB/etcd/Kafka 集群测试。下列为本机小样本测试，耗时只测一次，不代表生产吞吐。

## 压缩效果

容量采用十进制 KB/MB。新版大小包含压缩容器、内部恢复映射及外部回执；不含工作台日志、报告及尚保留的原件。对照基线先做精确文件去重，再取逐文件 GZIP 与原始内容中较小者，不计路径清单，因此它是偏低的保守基线，不是同等完整的归档产品。

| 合成资料 | 原始大小 | 深度归档总大小 | 相对原始缩减 | 归档总耗时 | 原路径不可访问时恢复 |
| --- | ---: | ---: | ---: | ---: | --- |
| 140 个相似/重复 JSON | 501.34 KB | 18.46 KB | 96.32% | 0.496 秒 | 全部一致 |
| 100 个短小、不同的十六进制文本 | 20 KB | 16.24 KB | 18.82% | 0.305 秒 | 全部一致 |
| 4 个随机二进制文件 | 2.10 MB | 无净收益，丢弃候选 | 无 | 0.838 秒 | 未保留归档，不适用 |
| 3 个已 GZIP 压缩的随机文件 | 1.57 MB | 无净收益，丢弃候选 | 无 | 0.603 秒 | 未保留归档，不适用 |
| 3 个仅有插入/局部修改的二进制版本 | 3.15 MB | 1.05 MB | 66.58% | 0.871 秒 | 全部一致 |
| 3 张噪声 JPEG 测试图 | 768.45 KB | 765.63 KB | 0.37% | 0.268 秒 | 全部一致 |

相似 JSON 的保守基线为 44.923 KB，深度归档比它小 58.91%；本次生成器与上次 815 KB 演示集不同，不能直接比较两个百分比。小文本集合的基线为 14.077 KB，深度归档含恢复信息后反而比该不计元数据基线大 15.34%，不能据此断言相同恢复保障下逐文件方案一定更小，但足以说明必须把元数据作为算法选择的一部分。

版本化二进制文件的结果来自整批 XZ 压缩，不是新增 CDC 分布式管线的存储结果。

## 新模块复现的问题

### P0：新归档包不包含文件内容

`storage_archive.py:272` 的 `_create_tarball` 只将 manifest.json 放入包。本次打开新归档，成员列表仅为 `['manifest.json']`。

`storage_archive.py:344` 的恢复实际复制 original_path；当原路径不可访问时，恢复 0 个文件并失败。`verify_archive` 此时却返回 True，因为它只检查仍存在的原文件，未验证包中的数据。这种包的小体积不能计为压缩收益。原有 deep_archive 流程未发现这个问题。

### P0：新恢复模块错误报告成功、覆盖现有文件

`storage_restore.py:451` 依赖原文件直接复制；本地无真实后端且原路径不可访问时，2 个文件全部跳过，返回状态仍是 completed。

在 verify=True、overwrite=False 的测试里，目标目录预先放入的文件仍被覆盖。修改过源文件的测试也没有产生校验失败记录。应在逐文件成功校验后计数，跳过、失败、取消均应传播至最终状态，并实际执行覆盖控制。

### P0：CDC 丢失短输入

`cdc/chunker.py:87` 在长度小于 48 时直接返回，而 `rabin_chunks` 未补起始边界。实测长度 1—47 的输入均产生空 chunk 列表，拼接无法还原原数据。

### P0：目录信息丢失与同名覆盖

`storage_archive.py:217` 按文件自己的父目录计算相对路径，`a/same.txt` 与 `b/same.txt` 都记录成 `same.txt`。本次恢复报告完成 2 个文件，输出目录实际上只有 1 个。

### P1：分布式能力仍是骨架

`fpdb/client.py:76` 的 connect 只改连接标志；lookup 返回未找到，insert 返回模拟成功。`coordinator/node.py:153` 的 put 等方法未执行存储，锁获取返回随机 token。`scannode/worker.py` 的任务拉取仍返回 None。

网页 app.py 仍调用原单机 storage_core/deep_archive，新增模块没有进入这条可运行流程。因此既不能说新版已提升网页压缩率，也不能用单机测试证明 README 中的百节点与 PB 级能力。

### P1：性能与节省口径未被验证

CDC 对 2 MiB 随机数据的单次处理约 0.471 秒，约 4.45 MB/s，不能外推 10000 文件/秒。两份近似版本的实际唯一 chunk 字节去重比约 1.95:1，不含指纹等元数据；不支持对外宣称普遍 20:1—50:1。

`storage_archive.py` 的 compressed_bytes 本次仍为 0；dedup_ratio 按 unique_chunks×4096 估算，但 CDC 块长可变，不能表示实际存储缩减。scannode 的 CDC 路径一次性读取整文件，也尚未满足大文件有界内存目标。

## 建议实施顺序

### 第一阶段：可靠性门槛

1. 统一归档格式和入口，优先沿用已经通过独立恢复验证的 deep_archive；修复新模块后再接入。
2. 修复短输入分块、原目录映射、覆盖控制和成功状态；清理原路径后必须能凭归档恢复。
3. 增加源文件变化、缺块、损坏、断电/中断、同名文件、重复任务、已有目标文件测试。只有全部通过才开放生产数据替换。
4. 将 README 的已实现能力、占位接口和性能目标明确分开。

### 第二阶段：有效节省与总成本

1. 在全量压缩前抽样判断可压缩性，为随机内容、已压缩内容和低收益图片提供快速跳过；检测到跨版本相似性时仍保留整批压缩机会。
2. 在同样恢复信息与质量标准下比较“保留原文件、逐文件压缩、整批压缩、分块去重”，用实际总字节选择方案。设置最小净节省量及最小收益比例，避免为 JPEG 的 0.37% 收益承担大量计算。
3. 按资料集选择默认策略：结构化日志、导出文档、版本化备份优先；图像 OCR 单独作为有损信息提取产品验证，不与无损归档混算。
4. 建立完整收益账：原件、归档、指纹索引、工作台报告、缓存、备份副本、临时峰值和恢复副本，区分预计、实际分配容量变化与实际账单变化。
5. 基于已批准的具体资料规则，设计“生成→验证→灰度替换→延迟清理”的可追溯流程。没有替换和空间回收，现有归档只是额外副本。

### 第三阶段：再扩展到企业规模

先用一个持久化数据库和真实对象存储打通数据块写入、索引提交、独立读取恢复，再做多节点；不要同时引入尚未落地的多套集群。

分层优化必须区分容量减少与单价降低；接入真实云账户后，计算请求、最低计费单位、取回、迁移及副本费用。对照实际账单验证，而不是只用文件大小乘单价。

建议下一次试点收集目标企业获准提供的日志/导出文档、版本化文件及扫描件各一批，先跑 10—100GB 级受控测试，再根据通过结果扩大规模。验收至少涵盖独立恢复、峰值内存、临时空间、异常恢复、净收益和恢复时延。
